<p id="notice"><%= notice %></p>

<h2 class="">Places</h2>

<div class="">
<!---------------------------card----------------------->
<% @places.each do |place| %>
  <div class="d-flex bg-success">
	<div class="">
		<div class="col-lg-3 col-sm-6">
      <div class="card hovercard">
          <div class="cardheader">
            <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?zoom=17&size=400x300&markers=size:small%7Ccolor:red%7C#{place.latitude},#{place.longitude}&key=#{Rails.application.credentials.api_key_map}" %>
          </div>
          <div class="avatar">
            <img alt="" src="<%=place.image_url%>">
          </div>
          <div class="info">
              <div class="title">
                  <h4><%=place.name%></h4>
              </div>
              <div class="bottom">
                  <p><%=place.description%></p>
                  <img src=http://openweathermap.org/img/w/<%= @meteo.get_values(place.longitude, place.latitude)["weather"][0]["icon"]%>.png>
                  <%=(@meteo.get_values(place.longitude, place.latitude)["main"]["temp_min"]-273.15).round(0)%>°C, </div>
                <%= link_to 'Voir la carte', place, class: "button-one" %>
              </div>
            </div>
        </div>
    </div>
  </div>
<% end %>
</div>

<br>

<%= link_to 'New Place', new_place_path %>